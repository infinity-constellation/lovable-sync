# Reusable workflow: Sync Lovable source repo into production repo
#
# Called from production repos via workflow_call.
# Reads .lovable-sync.yml from the calling repo to determine source/target.
#
# Authentication: Supports two modes (pick one):
#   1. GitHub App (recommended): pass app_id + app_private_key secrets
#   2. PAT: pass source_repo_token secret
#
# Triggers:
#   - repository_dispatch from the Lovable repo (event: lovable-sync)
#   - workflow_dispatch for manual runs
#   - Called by other workflows via workflow_call
#
name: Lovable Sync

on:
  workflow_call:
    inputs:
      dry_run:
        description: "If true, report changes without creating a PR"
        type: boolean
        default: false
    secrets:
      source_repo_token:
        description: "GitHub PAT with read access to the source Lovable repo (legacy, use App instead)"
        required: false
      app_id:
        description: "GitHub App ID"
        required: false
      app_private_key:
        description: "GitHub App private key (PEM)"
        required: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    outputs:
      sync_needed: ${{ steps.sync.outputs.sync_needed }}
      sync_branch: ${{ steps.sync.outputs.sync_branch }}
      source_sha: ${{ steps.sync.outputs.source_sha }}
      files_changed: ${{ steps.sync.outputs.files_changed }}

    steps:
      - name: Generate GitHub App token
        id: app-token
        continue-on-error: true
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.app_id }}
          private-key: ${{ secrets.app_private_key }}
          owner: ${{ github.repository_owner }}

      - name: Resolve auth token
        id: auth
        run: |
          if [[ -n "$APP_TOKEN" ]]; then
            echo "token=$APP_TOKEN" >> "$GITHUB_OUTPUT"
            echo "mode=app" >> "$GITHUB_OUTPUT"
            echo "Using GitHub App token"
          elif [[ -n "$PAT_TOKEN" ]]; then
            echo "token=$PAT_TOKEN" >> "$GITHUB_OUTPUT"
            echo "mode=pat" >> "$GITHUB_OUTPUT"
            echo "Using PAT token"
          else
            echo "::error::No authentication configured. Provide either app_id + app_private_key or source_repo_token."
            exit 1
          fi
        env:
          APP_TOKEN: ${{ steps.app-token.outputs.token }}
          PAT_TOKEN: ${{ secrets.source_repo_token }}

      - name: Checkout production repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout lovable-sync tooling
        uses: actions/checkout@v4
        with:
          repository: infinity-constellation/lovable-sync
          path: .lovable-sync
          token: ${{ steps.auth.outputs.token }}

      - name: Install yq
        uses: mikefarah/yq@v4

      - name: Validate sync config
        run: |
          if [[ ! -f .lovable-sync.yml ]]; then
            echo "::error::Missing .lovable-sync.yml in repository root"
            exit 1
          fi
          echo "Sync config found:"
          cat .lovable-sync.yml

      - name: Run sync
        id: sync
        env:
          GITHUB_TOKEN: ${{ steps.auth.outputs.token }}
        run: |
          chmod +x .lovable-sync/scripts/sync.sh
          .lovable-sync/scripts/sync.sh "${{ github.workspace }}"

      - name: Create Pull Request
        if: steps.sync.outputs.sync_needed == 'true' && inputs.dry_run != true
        env:
          GH_TOKEN: ${{ steps.auth.outputs.token }}
        run: |
          # Read PR config from .lovable-sync.yml
          TARGET_BRANCH=$(yq '.target_branch // "staging"' .lovable-sync.yml)
          SOURCE_REPO=$(yq '.source_repo' .lovable-sync.yml)
          LABELS=$(yq -r '.pr.labels // [] | join(",")' .lovable-sync.yml)
          ASSIGNEES=$(yq -r '.pr.assignees // [] | join(",")' .lovable-sync.yml)
          AUTO_MERGE=$(yq '.pr.auto_merge // false' .lovable-sync.yml)

          PR_TITLE="sync: Lovable update from ${SOURCE_REPO}@${{ steps.sync.outputs.source_sha }}"

          read -r -d '' PR_BODY << 'PRBODY' || true
## Lovable Sync

Automated sync of Lovable-managed files from \`${SOURCE_REPO}\`.

**Source:** \`${SOURCE_REPO}@${{ steps.sync.outputs.source_sha }}\`
**Target:** \`${TARGET_BRANCH}\`
**Files changed:** ${{ steps.sync.outputs.files_changed }}

### Changes
\`\`\`
${{ steps.sync.outputs.diff_stat }}
\`\`\`

---
*This PR was created automatically by [lovable-sync](https://github.com/infinity-constellation/lovable-sync).*
PRBODY

          PR_ARGS=(
            --title "$PR_TITLE"
            --body "$PR_BODY"
            --base "$TARGET_BRANCH"
            --head "${{ steps.sync.outputs.sync_branch }}"
          )

          if [[ -n "$ASSIGNEES" ]]; then
            PR_ARGS+=(--assignee "$ASSIGNEES")
          fi

          PR_URL=$(gh pr create "${PR_ARGS[@]}")
          echo "Pull request created: ${PR_URL}"

          # Apply labels separately so missing labels don't prevent PR creation
          if [[ -n "$LABELS" ]]; then
            IFS=',' read -ra LABEL_ARRAY <<< "$LABELS"
            for label in "${LABEL_ARRAY[@]}"; do
              gh label create "$label" --repo "${{ github.repository }}" 2>/dev/null || true
            done
            gh pr edit "$PR_URL" --add-label "$LABELS" || echo "::warning::Could not apply labels: $LABELS"
          fi

          if [[ "$AUTO_MERGE" == "true" ]]; then
            gh pr merge "$PR_URL" --auto --squash
            echo "Auto-merge enabled for PR"
          fi

      - name: Dry run summary
        if: steps.sync.outputs.sync_needed == 'true' && inputs.dry_run == true
        run: |
          echo "## Dry Run Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Changes would be synced:" >> $GITHUB_STEP_SUMMARY
          echo "- Files changed: ${{ steps.sync.outputs.files_changed }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.sync.outputs.diff_stat }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: No changes summary
        if: steps.sync.outputs.sync_needed != 'true'
        run: |
          echo "## Sync Check" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "No changes detected — source and production are already in sync." >> $GITHUB_STEP_SUMMARY
